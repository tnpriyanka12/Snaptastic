<!DOCTYPE html>
<html>
  <head>
    <title>SnapchatApp</title>
    <%= favicon_link_tag 'favicon.ico' %>
    <link href="https://fonts.googleapis.com/css?family=Poiret+One" rel="stylesheet">
    <meta name="theme-color" content="#ffffff">
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= cloudinary_js_config %>
  </head>

  <body class="<%= controller.controller_name %> <%= controller.action_name %>">
    <header>
    <div class="row text-center">
      <!-- <img class="logo" src="/assets/logo/logo.png" /> -->
    </div>
    </header>
    <% if @current_user.present? %>
    <h2> Welcome, <%= @current_user.name %> </h2>
      <h3><%= link_to 'Click to See Your Profile', @current_user , class: "welcomeuser"%></h3>

      <%= link_to 'Logout', login_path, method: :delete %>
    <% else %>
      <%= link_to 'Login', login_path %>
    <% end %>
    <!-- <nav class="navbar navbar-default navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>

          <%= link_to 'SNAPTASTIC', root_path, class: "navbar-brand" %>

        </div>


        <div class="collapse navbar-collapse" id="myNavbar">
          <div >
            <% if @current_user.present? %>
              <h5><%= link_to @current_user.name, @current_user , class: "welcomeuser"%></h5>
            <% end %>
          </div>

          <ul class="nav navbar-nav navbar-right">
            <% if @current_user.present? %>
              <li><%= link_to 'Home', root_path %></li>
              <li><%= link_to 'Gallery', snaps_path %></li>
              <li><%= link_to 'Profile', @current_user %></li>

              <li><%= link_to 'Logout', login_path, method: :delete %></li>
            <% else %>
              <li>  <%= link_to 'Home', root_path %></li>
              <li><a href="#about">About Us</a></li>
              <li><a href="#services">Games</a></li>
              <li><a href="#portfolio">Our Values</a></li>
              <li>  <%= link_to 'Login', login_path %></li>

            <% end %>
          </ul>
        </div>
      </div>
    </nav> -->
    <%= yield %>
  </body>
</html>
